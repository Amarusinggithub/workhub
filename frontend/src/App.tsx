import { initializeTheme } from 'hooks/useApperance';
import Register from 'pages/auth/Register';
import './App.css';

function App() {
	return;
	<Register />;
}
// This will set light / dark mode on load...
initializeTheme();

export default App;
